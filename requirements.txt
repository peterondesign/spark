        <p className="text-sm text-gray-500 mt-2">          Try searching directly on <a href={url || `https://www.getyourguide.com/s/?q=${slug}+${city}`} target="_blank" rel="noopener noreferrer" className="text-blue-500 underline">GetYourGuide</a>        </p>      </div>    );  }  return (    <div className="space-y-6 bg-white rounded-lg border border-gray-200 p-6">      {source === 'alternative' || source === 'fallback' ? (        <div className="bg-blue-50 text-blue-700 p-3 rounded-md text-sm mb-4">          Showing recommendations based on your location and interests        </div>      ) : null}            {result.results && result.results.length > 0 && (        <div className="grid grid-cols-1 md:grid-cols-3 gap-4">          {result.results.map((item, index) => (            <div key={index} className="border border-gray-200 rounded-lg overflow-hidden shadow-sm hover:shadow-md transition-shadow">              <div className="relative h-40">                {item.image ? (                  <img                     src={item.image}                     alt={item.title}                    className="w-full h-full object-cover"                  />                ) : (                  <div className="w-full h-full bg-gray-100 flex items-center justify-center">                    <span className="text-gray-400">No image</span>                  </div>                )}              </div>              <div className="p-4">                <h3 className="font-semibold text-gray-800 mb-2">{item.title}</h3>                {item.price && (                  <p className="text-green-600 font-semibold text-sm">{item.price}</p>                )}                {item.rating && (                  <p className="text-amber-500 text-sm mb-3">                    {Array.from({ length: Math.floor(parseFloat(item.rating)) }).map((_, i) => (                      <span key={i} className="text-amber-500">★</span>                    ))}                    {item.rating}                  </p>                )}                <div className="mt-3">                  <a                     href={item.url}                     target="_blank"                     rel="noopener noreferrer"                    className="inline-block bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded text-sm transition-colors"                  >                    View Details                  </a>                </div>              </div>            </div>          ))}        </div>      )}            <p className="text-xs text-gray-400 mt-6">        Source: {result.searchUrl}      </p>    </div>  );}